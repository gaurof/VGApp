@using VGApp.ViewModels;
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - GameVault</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/VGApp.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.0/css/all.min.css" />
</head>
<body>
    @{
        string returnUrl = @Url.PageLink()!.Substring(Constants.DefaultURL.Length);
    }
    <header class="game-header">
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm">
            <div class="container">
                <a class="navbar-brand" asp-controller="Home" asp-action="Index">
                    <i class="fas fa-gamepad me-2"></i>GameVault
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        @if (User.IsInRole(Constants.AdminRoleName))
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Admin" asp-action="Index">
                                    <i class="fas fa-cog me-1"></i> Admin
                                </a>
                            </li>
                        }
                    </ul>

                    @if (User.IsInRole(Constants.AdminRoleName))
                    {
                        <ul class="navbar-nav flex-grow-1">
                            @returnUrl
                        </ul>
                    }

                    <ul class="navbar-nav">
                        @if (User.Identity!.IsAuthenticated)
                        {
                            <li class="nav-item">
                                <form asp-controller="Account" asp-action="Info" asp-route-returnUrl=@returnUrl>
                                    <button type="submit" class="nav-link btn btn-link">
                                        <i class="fas fa-user me-1"></i> @User.Identity.Name
                                    </button>
                                </form>
                            </li>
                            <li class="nav-item">
                                <form asp-controller="Account" asp-action="Logout" asp-route-returnUrl=@returnUrl>
                                    <button type="submit" class="nav-link btn btn-link">
                                        <i class="fas fa-right-from-bracket me-1"></i> Logout
                                    </button>
                                </form>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Account" asp-action="Login" asp-route-returnUrl=@returnUrl>
                                    <i class="fas fa-user me-1"></i> Login
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Account" asp-action="Register" asp-route-returnUrl=@returnUrl>
                                    <i class="fas fa-user-plus me-1"></i> Register
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container main-container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="game-footer border-top py-3">
        <div class="container d-flex justify-content-between align-items-center">
            <div>
                &copy; 2025 - GameVault
            </div>
        </div>
        @if (returnUrl.ToLower().Contains("deltarune"))
        {
            <a onclick="splat()" onscroll="splat()">
                <img src="~/lancer.png" />
            </a>
        }

    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
<script>
    function splat(){
      var splat = new Audio('/splat.mp3');
      splat.play();
      }
</script>